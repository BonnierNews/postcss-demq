{
  "name": "postcss-demq",
  "version": "0.3.0",
  "description": "PostCSS plugin to filter media queries",
  "keywords": [
    "postcss",
    "css",
    "postcss-plugin",
    "media-queries"
  ],
  "scripts": {
    "test": "jest --coverage && eslint .",
    "test-node-10": "jest --coverage"
  },
  "author": "Bonnier News",
  "license": "MIT",
  "repository": "BonnierNews/demq",
  "engines": {
    "node": "^10 || ^12 || >=14"
  },
  "devDependencies": {
    "clean-publish": "^3.4.5",
    "eslint": "^8.13.0",
    "eslint-plugin-jest": "^25.7.0",
    "jest": "^27.5.1",
    "jest-cli": "^27.5.1",
    "postcss": "^8.4.12"
  },
  "peerDependencies": {
    "postcss": "^8.0.0"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 2017
    },
    "env": {
      "node": true,
      "es6": true
    },
    "extends": [
      "eslint:recommended",
      "plugin:jest/recommended"
    ],
    "rules": {
      "jest/expect-expect": "off",
      "jest/valid-title": "off",
      "strict": "error",
      "no-var": "error",
      "prefer-const": "error",
      "comma-dangle": [
        "warn",
        "always-multiline"
      ],
      "quotes": [
        "warn",
        "single"
      ],
      "semi": [
        "warn",
        "always"
      ]
    }
  },
  "jest": {
    "testEnvironment": "node",
    "coverageThreshold": {
      "global": {
        "statements": 100
      }
    }
  }
}
